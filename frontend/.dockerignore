node_modules/
dist/
.env
.env.*
.env.example
# If .env.example is solely for local dev overrides and not base defaults for build,
# it can be excluded. If it contains non-sensitive build-time defaults, it could be included.
# For this project, .env.production sets VITE_API_BASE_URL for the build, so specific .env files are not needed in image.

.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vite specific cache
.vite/
# ESLint cache
.eslintcache

# IDE and OS specific
.vscode/
.idea/
*~
